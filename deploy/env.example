# Copy to repo root as `.env` for `docker compose` (see deploy/README.md)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
GITHUB_TOKEN=

# Optional: override default model
# EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
