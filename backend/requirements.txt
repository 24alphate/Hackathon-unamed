# API only — fits Vercel serverless (<500MB). For local Chroma + embeddings, also install
#   pip install -r requirements-ml.txt
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
python-dotenv>=1.0.0
openai>=1.60.0
pydantic>=2.9.0
